# Follow-on goal: complete the private/local V1 Release Candidate

Complete the private/local V1 Release Candidate through C11 on
`codex/c7-c11-program` in one continuous goal. Do not end after a milestone,
progress report, failed proof, benchmark miss, review finding, contract
correction, or local commit. Continue until C7-C11 and the integrated RC review
are complete, unless a later owner instruction, an irreconcilable controlling
Phase 1 conflict, or a hard system/tool/usage impossibility makes further
execution literally impossible.

First read `AGENTS.md`, `README.md`, `docs/v1-release-program.md`, this prompt,
`docs/implementation-process.md`, and `docs/specification-map.md`. Verify the
expected initial state: the corrected program documentation is staged
separately and preserved C7-S3 code/tests are unstaged/untracked. Commit only
the exact staged program-document paths as their own local authority milestone;
do not stage or alter unrelated work in that commit. If safe committing is
unavailable, preserve the exact split, record it, and continue because Git
bookkeeping is not a release-capability prerequisite.

Resume C7-S3, preserving the measured evidence: the 4.39 MB reference artifact
loaded in 9.82 seconds, install took 63 ms, and complete checkpoint restart took
22.53 seconds. Apply the current 6,000-symbol/equivalent-fresh bounded proof and
correct codec, implementation, fixture, benchmark, proof, slice, whitelist, or
lower-level contract decisions as evidence requires. The current 60-second and
20%-faster thresholds are program-selected acceptance settings, revisable from
measured evidence only; any replacement must remain materially faster than the
equivalent fresh path and below the Phase 1 rejected approximately 130-second
precedent. Reopen C7-S1/S2 only where implicated and preserve unaffected
evidence. Finish C7 proof and final review.

Then complete, implement, prove, review, accept, and locally commit C8, C9, C10,
and C11 sequentially from their current V1 boundary plans. Keep contracts
compact and use no more than two slices unless a third protects a distinct
consequential boundary. Use one active implementation slice and at most one
writer; reviewers are read-only and run only when required. Record
program-authorized boundary and completed-contract conformance decisions
yourself. Never ask the owner to approve an in-scope C7-C11 lower-level
decision.

For every correctable failure, preserve the evidence, identify the failed
claim, revise the lowest unsuitable artifact, run the narrowest distinguishing
proof, correct review findings, update the ledger, and continue. Never rerun an
unchanged failed command. After two failed attempts on the same mechanism or
premise, abandon it for the simplest fixed-authority-compliant alternative and
rebuild from a compact deterministic proof before rerunning scale. Substitute
and record an available reviewer model if the preferred model is unavailable;
if reviewer capacity is temporarily unavailable, complete other work and retry
later.

Credentialed/live-provider work, public deployment, authentication/TLS/
hosting, destructive or external actions, and market-hours validation remain
deferred and are never prerequisites to this RC. Use deterministic replay,
fake-provider, loopback, and local evidence while the market is closed. Record
`docs/market-hours-validation.md` as pending. Preserve unrelated user changes;
work around safe same-path conflicts and continue without a milestone commit if
necessary rather than overwriting, combining unrelated work, or rewriting
history.

Ordinary verification is `go test -short -timeout 2m ./...`; acceptance,
capacity, replay, and race commands are explicit and bounded. Stage exact paths
and make local commits only with all workers/reviewers quiescent. Finish with
the compact deterministic end-to-end scanner/checkpoint/TQ/API/UI scenario,
one integrated read-only review, current ledgers/specification map,
market-hours validation recorded pending, and a locally verified private V1
RC. Do not push or rewrite history.
